# Backend API base URL (without trailing slash)
# In development, the Vite dev server proxies /api to this URL
# In production, set this to the full backend URL or leave empty for same-origin
VITE_BACKEND_API_URL=

# Backend API path prefix (defaults to /api/v2 if not set)
VITE_BACKEND_API_PATH=/api/v2
